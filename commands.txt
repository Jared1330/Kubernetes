# version server and client
kubectl version  
# version of client
kubectl version -- client 
# состояние k8s cluster
kubectl get componentstatus 
# info of k8s cluster
kubectl cluster-info 
# show all servers of k8s cluster
kubectl get nodes 


# Создать и Запустить K8s Cluster с параметрами по умолчанию
eksctl create cluster --name test
 
